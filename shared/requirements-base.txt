# AgroVision — Shared base requirements for all FastAPI services
# Each service pins this file via -r ../../shared/requirements-base.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Validation
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1

# Authentication (used by identity-service; JWT verification in others)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Messaging
aio-pika==9.4.1

# Cache
redis[asyncio]==5.0.4

# HTTP client (inter-service calls)
httpx==0.27.0

# Logging
structlog==24.1.0

# Testing (install separately in CI: pip install pytest pytest-asyncio)
# pytest==8.2.0
# pytest-asyncio==0.23.6

# Linting (install separately in CI: pip install ruff)
# ruff==0.4.4
