/**
 * Zustand store index.
 * Each domain has its own slice. Import individual stores in components.
 */

export { useAuthStore } from './slices/authSlice';
export { useNotificationStore } from './slices/notificationSlice';
